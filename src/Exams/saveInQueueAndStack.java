package Exams;

import java.util.ArrayDeque;
import java.util.Arrays;

public class saveInQueueAndStack {
    public static void main(String[] args) {

//        ArrayDeque<Double> milkQueue = Arrays.stream(scanner.nextLine().split(" "))
//                .map(Double::parseDouble).collect(Collectors.toCollection(ArrayDeque::new));
//        ArrayDeque<Double> cacaoStack = new ArrayDeque<>();
//        Arrays.stream(scanner.nextLine().split(" "))
//                .map(Double::parseDouble).forEach(cacaoStack::push);
    }
}
